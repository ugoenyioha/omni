# Example TSIG key file for DDNS updates
# Copy this file to tsig.key and update with your actual key
#
# To generate a new TSIG key:
# dnssec-keygen -a HMAC-SHA256 -b 256 -n HOST omni-ddns
#
# The key name should match your DNS server configuration

key "omni-ddns" {
    algorithm hmac-sha256;
    secret "REPLACE-WITH-YOUR-BASE64-ENCODED-SECRET";
};